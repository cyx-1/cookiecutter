[project]
name = "cookiecutter-aws-lambda"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
dev-dependencies = [
    "black",
    "flake8",
    "mypy"
]
